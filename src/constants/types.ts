export interface BookItem {
  title: string;
  author: string,
  publishedYr:string,
  coverImg:string
}

export interface Book {
  title: string;
  author: string,
  publishedYr:string,
  coverImg:string,
}
